# Core Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Pydantic & Settings
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator>=2.0.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Rate Limiting
slowapi==0.1.9

# Redis (async)
redis[hiredis]==5.2.1

# Email
# google provider  → SMTP transport
aiosmtplib==3.0.2
# klaviyo provider → Events API over HTTP (also used by the test client)
httpx==0.28.1

# Background jobs (app/services/scheduler_service.py: retention sweep, outbox
# drain, daily reminders). Without this the app fails at import time on a fresh
# install, so it is pinned rather than optional.
apscheduler==3.11.0

# Utilities
python-multipart==0.0.12
# Object-storage backend for uploads (STORAGE_BACKEND=s3; optional at runtime —
# imported lazily so local-disk deployments don't need it installed)
boto3==1.35.90
# Windows requires tzdata for Python's zoneinfo module (no system tz database)
tzdata>=2024.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
# Embedded Postgres for the real integration suites (tests skip when absent)
pgserver==0.1.4
